

i18n.merge_file(
    input: 'terminal-plugin.desktop.in',
    output: 'terminal.plugin',
    po_dir: join_paths(meson.source_root(), 'po'),
    type: 'desktop',
    install: true,
    install_dir: pluginslibdir,
)
